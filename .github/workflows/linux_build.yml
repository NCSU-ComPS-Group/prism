name: linux-build

on:
  push:
    branches:
      - main

jobs:
  linux-build:
    runs-on: ubuntu-latest
    # Set up a default shell for all steps
    # See: https://github.com/marketplace/actions/setup-miniconda#use-a-default-shell
    defaults:
      run:
        shell: bash -el {0}
    steps:
    - name: Checkout branch
      uses: actions/checkout@v4
      with:
        submodules: true
    - name: Set up Mambaforge3 and conda environment
      uses: conda-incubator/setup-miniconda@v3
      with:
        miniforge-variant: Mambaforge
        use-mamba: true
        activate-environment: rxn-cpp
        environment-file: scripts/linux_env.yml
    # GitHub-hosted runners are currently limited to 4 cores
    # See: https://docs.github.com/en/actions/using-github-hosted-runners/about-github-hosted-runners#supported-runners-and-hardware-resources
    - name: Build and run tests
      run: |
        conda activate rxn-cpp
        export LD_LIBRARY_PATH="$CONDA_PREFIX/lib:$LD_LIBRARY_PATH"
        cd test
        make -j 2
        ./run_tests
        diff -y output/latex_table1.txt gold/latex_table1.txt
        diff -y output/latex_table2.txt gold/latex_table2.txt
        diff -y output/rxn_summary1.yaml gold/rxn_summary1.yaml
        diff -y output/rxn_summary2.yaml gold/rxn_summary2.yaml
        diff -y output/species_summary1.yaml gold/species_summary1.yaml
        diff -y output/species_summary2.yaml gold/species_summary2.yaml

